import { ImageSourcePropType } from "react-native";

export type ButtonProps = {
  style: object;
  handlePress: () => void;
  imageSource: ImageSourcePropType;
  // textStyles?: string;
  // isLoading?: boolean;
//   imageSource: string;
};
